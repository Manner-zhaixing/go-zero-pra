syntax = "v1"

type userReq {
	userId int64 `json:"userId"`
}

type userResp {
	userId int64  `json:"userId"`
	Name   string `json:"userName"`
}

service Foo {
	// 定义 path 参数
	@handler userInfo
	post /api/userInfo/:id (userReq) returns (userResp)
}

